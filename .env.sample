# Environment Configuration Template
# Copy this file to .env and fill in your actual values
# DO NOT commit .env to version control

# Local Development with LocalStack
# Most configuration is handled by the Makefile and Terraform
# For local development, simply run: make start && make deploy

# Production AWS Configuration (optional, for production deployments only)
AWS_PROFILE=YOUR_AWS_PROFILE
AWS_REGION=us-east-1
AWS_ACCOUNT_ID=YOUR_AWS_ACCOUNT_ID

# Environment name (development, staging, production)
ENVIRONMENT=development

# LocalStack Development
# These values are automatically configured by Terraform for LocalStack
# See terraform/local/outputs.tf for generated values
# Access outputs with: cd terraform/local && terraform output

# Production Deployment Configuration (optional)
# Only needed for production AWS deployments

# Tenant Configuration Table (auto-configured by Terraform/CloudFormation)
# TENANT_CONFIG_TABLE=multi-tenant-logging-${ENVIRONMENT}-tenant-configs

# Central IAM Role (production only)
# CENTRAL_LOG_DISTRIBUTION_ROLE_ARN=arn:aws:iam::${AWS_ACCOUNT_ID}:role/central-log-distributor

# Vector Integration Testing (production environments only)
# S3_WRITER_ROLE_ARN=arn:aws:iam::${AWS_ACCOUNT_ID}:role/s3-writer-role
# S3_BUCKET_NAME=central-logs-bucket

# Optional: For testing with real AWS credentials (NOT recommended for local dev)
# AWS_ACCESS_KEY_ID=your_access_key_here
# AWS_SECRET_ACCESS_KEY=your_secret_key_here

# Development Notes:
# - Use 'make start' to start LocalStack for local development
# - Use 'make deploy' to deploy infrastructure locally
# - Use 'make help' to see all available commands
# - Terraform outputs provide all necessary configuration values
