apiVersion: v1
kind: Service
metadata:
  name: tenant-config-api
  namespace: logging
spec:
  selector:
    app: tenant-config-api
  ports:
  - name: http
    port: 8080
    targetPort: 8080
  type: ClusterIP