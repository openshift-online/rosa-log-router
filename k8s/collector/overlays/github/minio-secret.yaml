apiVersion: v1
kind: Secret
metadata:
  name: minio-credentials
  namespace: default
type: Opaque
stringData:
  # Test credentials for S3-compatible storage
  access-key: "testadmin"
  secret-key: "testadmin"