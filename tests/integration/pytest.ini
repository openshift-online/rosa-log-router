[tool:pytest]
markers =
    integration: marks tests as integration tests requiring DynamoDB Local
    slow: marks tests as slow running
    api: marks tests as API-related
    concurrent: marks tests that test concurrent operations